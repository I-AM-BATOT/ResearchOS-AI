# ResearchOS AI - dependencies
# Python 3.11+

# Core app
streamlit>=1.40.0
pydantic>=2.7.0
python-dotenv>=1.0.1

# LLM
google-generativeai>=0.8.0
# google-adk>=0.1.0   # optional: uncomment to wire in a real Google ADK Runner (see core/llm_client.py NOTE)

# MCP server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client for arXiv / Semantic Scholar
requests>=2.32.0

# Memory
chromadb>=0.5.0   # optional: falls back to SQLite automatically if not installed

# PDF processing
pypdf>=5.0.0

# Testing
pytest>=8.3.0
pytest-cov>=5.0.0
